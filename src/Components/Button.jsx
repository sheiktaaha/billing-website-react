import React from "react";

const Button = () => {
  return (
    <div>
      <button
        type="button"
        className="inline-flex items-center px-12 py-1 border border-transparent text-xs font-medium rounded shadow-sm text-white bg-indigo-600 focus:outline-none"
      >
        Save
      </button>
    </div>
  );
};

export default Button;
